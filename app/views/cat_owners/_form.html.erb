<%= form_with(model: cat_owner, local: true) do |form| %>
  <% if cat_owner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cat_owner.errors.count, "error") %> prohibited this pet_owner from being saved:</h2>

      <ul>
      <% cat_owner.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cat_id %>
    <%= form.select(:cat_id, @cat_select_list, {include_blank: true}) %>
  </div>

  <div class="field">
    <%= form.label :owner_id %>
    <%= form.select(:owner_id, @owner_select_list, {include_blank: true}) %>
  </div>

  <%= form.hidden_field :cat_status %>
  <%= form.hidden_field :owner_status %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
