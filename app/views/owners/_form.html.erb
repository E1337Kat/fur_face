<%= form_with(model: owner, local: true) do |form| %>
  <% if owner.errors.any? %>
    <div id='error_explanation'>
      <h2><%= pluralize(owner.errors.count, 'error') %> prohibited this owner from being saved:</h2>

      <ul>
      <% owner.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class='field'>
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class='field'>
    <%= form.label :actually_a_catgirl %>
    <%= form.check_box :actually_a_catgirl %>
  </div>

  <div class='field'>
    <%= form.label :location %>
    <%= form.select(:location, Owner.locations.keys.map {|location| [location.titleize,location]}) %>
  </div>

  <div class='actions'>
    <%= form.submit %>
  </div>
<% end %>
